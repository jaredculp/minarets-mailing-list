# minarets-mailing-list

A couple of scripts to format the raw mailing list txt file I received from DMBAlmanac into more readable HTML

## Build

You'll need `awk` and `csplit` installed. Then a simple:
```
$ ./build.sh
```
